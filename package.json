{
  "name": "image-ai-server",
  "version": "1.0.0",
  "description": "Image AI description server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build step - Flutter web already built'",
    "deploy": "npm install && npm start"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
